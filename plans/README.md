# Implementation Plans

Generated by the improve skill on 2026-06-13. Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|---|---|---|---|---|---|
| 001 | Upgrade vulnerable runtime dependencies | P1 | M | - | DONE (verified 2026-06-18 @ `d69ad4a`, merged) |
| 002 | Move CLI-only shadcn out of production dependencies | P1 | S | 001 | DONE (verified 2026-06-18 @ `d69ad4a`, merged) |
| 003 | Hide archived and deleting repositories from active selection | P1 | S | - | DONE (verified 2026-06-18 @ `d69ad4a`, merged via #152) |
| 004 | Remove fixed-slice filtering from active repository lists | P2 | S/M | 003 | DONE (verified 2026-06-18 @ `d69ad4a`, merged) |
| 005 | Bound sandbox session cost summary reads | P2 | M | - | DONE (verified 2026-06-18 @ `d69ad4a`, merged) |
| 006 | Prune stored artifact version history and orphaned HTML blobs | P2 | M | - | TODO (refreshed 2026-06-24 @ `23c0a02`; approved commit `e9b6bc2` exists on branch `worktree-agent-a363a7c418dc16742` but is not merged into `main`) |
| 007 | Add test coverage for the HTML draft validation-repair loop | P2 | M | - | DONE (merged via #157; targeted tests pass 2026-06-24; full typecheck/lint still blocked by pre-existing `convex/daytona.ts:183` SDK type errors) |
| 008 | Index `findArtifactInFolderByKind` (drop unbounded folder scan) | P3 | S | - | DONE (merged in `8263672`; targeted tests pass 2026-06-24; full typecheck/lint still blocked by pre-existing `convex/daytona.ts:183` SDK type errors) |
| 009 | Make `htmlValidationStatus` reflect real validation state | P3 | S | - | DONE (merged via #158; targeted tests pass 2026-06-24; full typecheck/lint still blocked by pre-existing `convex/daytona.ts:183` SDK type errors) |
| 010 | Re-triage Daytona runtime dependency advisories | P1 | M | - | TODO (approved execution commit `9822290` exists on branch `advisor/010-retriage-daytona-runtime-dependencies` but is not merged into current branch) |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale).

## Audit log — 2026-06-27 (at HEAD `5d75710`)

A standard `/improve` follow-up rechecked the repository after the Design Docs
and Library-mode changes. Verification now passes globally:

- `bun run typecheck` passed.
- `bun run typecheck:convex` passed.
- `bun run lint` passed.
- `bun run test` passed: 129 files, 1660 tests. The known JSDOM `scrollTo` and
  `TimeoutNegativeWarning` warnings still appeared but did not fail tests.

Selected plans from this pass:

- **006 — still the first executable item.** Current `main` still has no
  `MAX_ARTIFACT_VERSIONS` / `pruneArtifactVersions` helper. The already-approved
  implementation commit `e9b6bc2` remains the lowest-friction path, but it is
  still not merged.
- **010 — new.** `bun audit --production` now reports 41 advisories (15 high,
  22 moderate, 4 low). The high production paths are concentrated around
  `@daytona/sdk` transitives and `convex -> ws`, while read-only registry probes
  show newer direct versions exist for `@daytona/sdk`, `convex`, and `mermaid`.
  Plan 010 upgrades direct runtime packages first, then documents any remaining
  high advisories by reachability and upstream blocker.

Deferred from this pass: typed Convex environment variables. The generated
Convex guidance recommends declaring env vars in `convex/convex.config.ts`, but
the migration touches many runtime and test call sites. It is worthwhile after
006/010, not ahead of them.

### Execution follow-up — 010 (2026-06-27)

Plan 010 was dispatched to a worker executor in an isolated worktree and
reviewed: **APPROVED**.

- Commit `9822290` "Re-triage Daytona runtime dependencies" — 2 files:
  `package.json` and `bun.lock`. It updates `@daytona/sdk` to `^0.191.0`,
  `convex` to `^1.42.0`, `mermaid` to `^11.16.0`, and aligns the existing
  Mermaid override.
- Lives on branch `advisor/010-retriage-daytona-runtime-dependencies` at
  `/Users/eric/personal-project/systify/.codex-worktrees/advisor-010-retriage-daytona-runtime-dependencies`,
  **not merged** — merging is the maintainer's call.
- Review caveat: the isolated worktree's default `PATH` selected Node
  `v18.15.0`, which made jsdom 29 fail to start because `@exodus/bytes` requires
  Node `^20.19.0 || ^22.12.0 || >=24.0.0`. Verification was rerun with the same
  Node selected by the main worktree (`v25.8.1`).
- Verified independently in the worktree with Node `v25.8.1`: `bun run format`,
  `bun run lint`, `bun run test -- daytona daytonaRetry`,
  `bun run test -- mermaid`, `bun run test -- withLlmRetry`, and full
  `bun run test` all exit 0. Full tests passed: 129 files, 1660 tests; known
  JSDOM `scrollTo` warnings appeared.
- `bun audit --production` still exits nonzero: 39 vulnerabilities (14 high,
  21 moderate, 4 low). Residual high advisories remain under
  `@daytona/sdk@0.191.0`: `form-data` via SDK upload methods not used by
  Systify; `@grpc/grpc-js` and `protobufjs` via Daytona OpenTelemetry exporters
  while Systify's `createDaytonaClient` does not enable Daytona OTel; and
  `fast-xml-builder` via Daytona AWS/S3 snapshot/object-storage helpers not
  called by Systify. Current patched transitive versions exist
  (`form-data@4.0.6`, `@grpc/grpc-js@1.14.4`, `protobufjs@8.6.5`,
  `fast-xml-builder@1.2.0`), but the latest direct Daytona package is `0.191.0`
  and still produces these audit paths. Convex `ws` is resolved to `8.21.0`;
  the previous `ws` high advisory is cleared.

## Reconcile log — 2026-06-24 (at HEAD `23c0a02`)

A `reconcile` pass compared the active 2026-06-24 plans against current `main`.
Outcome:

- **006 — TODO, refreshed.** The approved implementation commit `e9b6bc2` still exists on branch `worktree-agent-a363a7c418dc16742` / `origin/worktree-agent-a363a7c418dc16742`, but it is not in current `main`. Current HEAD still has no `MAX_ARTIFACT_VERSIONS` / `pruneArtifactVersions` helper, and `updateArtifactWrite` still patches the artifact then schedules reindexing without pruning stored `artifactVersions`. The finding therefore still exists. The plan was refreshed from `c7b6aac` to `23c0a02` to account for the already-merged 008/009 edits in `convex/lib/artifactWrites.ts` and `convex/schema.ts`.
- **007 — DONE with global gate caveat.** Merged via #157 (`adddeb6`). The two repair-loop tests are present in `convex/libraryArtifactDraftsNode.test.ts` and assert the initial `"library_html_report_draft"` call, the `"library_html_report_repair"` calls, usage accumulation, and failed-state behavior after the max repair attempts.
- **008 — DONE with global gate caveat.** Merged in `8263672`. `convex/schema.ts` defines `by_repositoryId_and_folderId_and_kind`, and `findArtifactInFolderByKind` now uses that index with `.first()` instead of collecting the whole folder.
- **009 — DONE with global gate caveat.** Merged via #158 (`2324251`). `createArtifactVersionWrite` now derives `htmlValidationStatus` from `htmlValidationErrors`, and `convex/artifactVersions.test.ts` covers valid HTML, invalid HTML, and markdown.

Verification run during reconcile:

- `bun run test -- libraryArtifactDraftsNode artifactVersions artifactStore` passed: 3 files, 34 tests.
- `bun run typecheck` and `bun run typecheck:convex` both failed only on the pre-existing Daytona SDK typing mismatch at `convex/daytona.ts:183`:
  `daytona.list({ labels, limit: 100 })` is typed as `Promise<PaginatedSandboxes>`, with `labels` / `limit` expected as strings by the installed SDK types.
- Because `bun run lint` starts with those typecheck commands, the lint/full completion gate remains globally blocked until the Daytona type issue is fixed.

Executable right now: **006**. The existing implementation branch can be cherry-picked or re-executed from the refreshed plan; `git merge-tree c7b6aac HEAD e9b6bc2` reports an auto-mergeable result, but the user should decide whether to merge that branch or dispatch a fresh executor.

## Audit log — 2026-06-24 (at HEAD `c7b6aac`)

A bare `/improve` (standard) run focused on the **HTML artifact versioning**
feature merged since the 2026-06-18 audit (`htmlArtifacts.ts`, `artifactWrites.ts`,
`artifactVersions.ts`, `libraryArtifactDrafts*.ts`, schema, `library-editor.tsx`),
plus a light broad perf sweep. Four findings were vetted against live code and
turned into plans 006–009 (all selected by the maintainer).

Confirmed safe, no action: HTML artifact XSS is well-mitigated — previews render
in a fully-locked `<iframe sandbox="" referrerPolicy="no-referrer">` served from
storage (`src/components/library-editor.tsx:272`) and `htmlArtifacts.ts` injects a
strict CSP and rejects `<script>`/event handlers/`javascript:`. Ownership/IDOR on
the new version + draft mutations is enforced (`loadOwnedDoc`; `getVersion` checks
`ownerTokenIdentifier`).

**Not audited this pass:** the previously-cleared surface (chat streaming, sandbox
provisioning, GitHub import, rate limiting — see 2026-06-18 log), the newer
chat-mode-controls / thread-search UI, and `bun audit` deps (prior advisories
documented as unreachable below).

### Execution follow-up — 006 (2026-06-24)

Plan 006 was dispatched to a `sonnet` executor in an isolated worktree and reviewed (criteria re-run, diff read, tests audited): **APPROVED**.

- Commit `e9b6bc2` "Prune stored artifact version history and orphaned HTML blobs" — 2 files: `convex/lib/artifactWrites.ts` (+`MAX_ARTIFACT_VERSIONS = 50`, `pruneArtifactVersions` helper, call from `updateArtifactWrite` only when a new version was created) and `convex/artifactStore.test.ts` (+4 tests: prune-beyond-cap asserts exact math — 50 retained, lowest v6 / highest v55; under-cap retains all; current version survives and matches; shared HTML blob not deleted while still referenced).
- Lives on worktree branch `worktree-agent-a363a7c418dc16742` at `/Users/eric/personal/systify/.claude/worktrees/agent-a363a7c418dc16742`, **not merged** — merging is the maintainer's call. Stays in scope (only the two in-scope files; no schema/index/listing-query changes).
- Verified independently in the worktree: `typecheck`, `typecheck:convex`, `lint` (0 warnings) all exit 0; targeted tests 31/31 pass. The one full-suite failure (`src/pages/history.test.tsx`) is a pre-existing parallelism flake — passes 35/35 in isolation and is a UI test unrelated to this backend-only change.
- Deferred (noted in the plan's Maintenance notes, not done here): a one-time backfill migration to prune history that already exceeds the cap on existing artifacts. This plan only bounds growth going forward.

## Reconcile log — 2026-06-18 (at HEAD `d69ad4a`)

A `reconcile` pass verified every plan's done criteria against the live code on HEAD. **All five plans are DONE and merged into `main`.** The prior 2026-06-13 reconcile log (below) had reverted 003/004/005 to TODO because they were marked DONE without execution; they have since been executed and merged. Per-plan verification on `d69ad4a`:

- **001 — DONE.** `package.json` carries `@daytona/sdk@0.187.0`, `react-router-dom@^7.17.0`, `mermaid@^11.15.0`, and `overrides` for `shell-quote@^1.8.4` + `mermaid@^11.15.0`; `convex/daytona.ts:183` uses the async iterator (`for await (const sandbox of daytona.list({ labels, limit: 100 }))`). The remaining-advisories note below still describes live state.
- **002 — DONE.** `shadcn@^4.2.0` is under `devDependencies` in `package.json`.
- **003 — DONE.** Index `by_owner_delete_archive_lastAccessedAt` exists (`convex/schema.ts:306`); `listRepositoriesForSwitcher` queries it with `deletionRequestedAt === undefined` + `archivedAt === undefined` (`convex/repositoryPreferences.ts:28-32`); `listOwnedRepositoryIdsById` excludes archived/deletion rows (`:64-65`); `convex/repositoryPreferences.test.ts` adds the switcher/ownership-probe coverage.
- **004 — DONE.** Index `by_owner_delete_archive_importedAt` exists (`convex/schema.ts:293`); `listRepositories`, `listResourceInventory`, and `getImportedRepoSummaries` all query it with `.eq("archivedAt", undefined)` (`convex/repositories.ts:46,75,181`); no `repositories.filter((repo) => repo.archivedAt …)` remnant remains; regression tests added in `convex/repositories.archive.test.ts`.
- **005 — DONE.** `getSandboxSessionCostSummary` (`convex/sandboxSessions.ts:112`) no longer `.collect()`s all sessions — it aggregates today's spend via a ranged `for await` over `by_repositoryId_and_startedAt` with `gte("startedAt", todayStart)` (`:124-127`) and finds the current session by status; tests extended in `convex/sandboxSessions.test.ts`.

The backlog is empty — no TODO/BLOCKED/IN PROGRESS plans remain. A fresh `/improve` audit on 2026-06-18 found no new high-leverage work to plan (see "Findings considered and rejected").

## Reconcile log — 2026-06-13 (at HEAD `6f1b32c`)

A `reconcile` pass verified each DONE claim against the actual code on HEAD. Findings:

- **001 — confirmed DONE.** Commit `6f1b32c` carries the dependency upgrades (`@daytona/sdk@0.187.0`, `react-router-dom@^7.17.0`, `mermaid@^11.15.0`, plus `overrides` for `shell-quote@^1.8.4` and `mermaid`) and the `convex/daytona.ts` list-API migration to the async iterator (`for await (const sandbox of daytona.list(...))` at `convex/daytona.ts:183`). The remaining-advisories note below still describes the live state.
- **002 — confirmed DONE.** `shadcn` now sits under `devDependencies` in `package.json` (line ~94, beside `prettier`). Same commit `6f1b32c`.
- **003 / 004 / 005 — reverted to TODO.** These were marked DONE but never executed: no commits, no `advisor/003|004|005` branches, clean working tree. The code on HEAD is unchanged from the planning commit (drift check `962761d..HEAD` is empty for all in-scope files) and each finding is fully intact:
  - 003: `listRepositoriesForSwitcher` (`convex/repositoryPreferences.ts:22`) still queries the owner-only `by_ownerTokenIdentifier_and_lastAccessedAt` index with no archived/deletion filter; no `by_owner_delete_archive_lastAccessedAt` index exists. The committed `convex/repositoryPreferences.test.ts` only covers `listOwnedRepositoryIdsById` ownership normalization — it does **not** cover archived/deletion exclusion, so the plan's Step 4 still applies (extend the file, don't create it).
  - 004: `convex/repositories.ts:52` and `:78` still `.take(REPOSITORY_LIST_TAKE)` then `.filter((repo) => repo.archivedAt === undefined)`; no `by_owner_delete_archive_importedAt` index exists.
  - 005: `getSandboxSessionCostSummary` (`convex/sandboxSessions.ts:118`) still `.collect()`s every session for the repository.

  Plan excerpts and line numbers still match HEAD, so no refresh was needed; the `962761d` drift baseline in each plan remains valid. All three are executable now (003 first, then 004 which depends on it; 005 is independent).

### Execution follow-up — 003 (2026-06-13)

Plan 003 was dispatched to an executor in an isolated worktree and reviewed (criteria re-run, diff read, tests audited): **APPROVED**.

- Commit `c7e4e7a` "Hide archived repositories from switcher" — 3 files: `convex/schema.ts` (+index `by_owner_delete_archive_lastAccessedAt`), `convex/repositoryPreferences.ts` (switcher + ownership-probe now exclude archived/deletion-requested rows), `convex/repositoryPreferences.test.ts` (+6 tests).
- Lives on worktree branch `worktree-agent-ad5f3d44eae0e7c8c`, **not merged** — merging is the maintainer's call.
- The worktree was branched from `main` (962761d), so a full branch diff appears to also drop 001/002/plans; that is a base artifact. The commit `c7e4e7a` itself touches only the 3 files above, which 001/002 never modified — cherry-picking it onto the current branch is conflict-free.
- **004 is now unblocked** (its dependency 003 is done).

## Dependency Notes

- 002 follows 001 because both edit `package.json` and `bun.lock`; dependency upgrades should settle before dependency classification.
- 004 follows 003 because both touch repository lifecycle/listing indexes and active repository semantics. 003 handles active selection first; 004 handles broader active listing correctness.
- 005 is independent and can run after the P1 plans or in parallel in a separate worktree if the operator supports that.
- 006 is the only currently executable plan from the 2026-06-24 batch. 007, 008, and 009 have landed. Plan 006 touches `convex/lib/artifactWrites.ts` and `convex/artifactStore.test.ts`; when merging the previously-approved `e9b6bc2` implementation, preserve the already-merged 008 folder-kind index lookup and 009 truthful `htmlValidationStatus` changes.
- Recommended current order from the 2026-06-27 follow-up: finish/merge 006
  first because it is already approved, then merge approved 010. 010 has no
  code dependency on 006, so either branch can be merged independently if
  needed.

## Verification Baseline

At audit time:

- `bun run typecheck` passed.
- `bun run typecheck:convex` passed.
- `bun run lint` passed.
- `bun run test` passed: 129 files, 1660 tests.
- `bun audit --production` failed with 41 advisories: 15 high, 22 moderate, 4 low.
- `bun audit` failed with 71 advisories: 22 high, 41 moderate, 8 low.
- `bun run format` was not run during audit because this skill is read-only except for files under `plans/`; executors must run it before completing code changes.

Repo-required completion commands for code changes:

```sh
bun run format
bun run lint
bun run typecheck
bun run test
```

Do not run dev servers or builds unless explicitly asked.

## Plan 001 — remaining advisories after upgrade (documented, no reachable runtime fix)

Post-fix `bun audit`: 40 vulnerabilities (14 high, 24 moderate, 2 low). Direct runtime targets resolved: `react-router-dom@^7.17.0`, `@daytona/sdk@0.187.0` (list API migrated to async iterator in `convex/daytona.ts`), `mermaid@^11.15.0` (also forced on `streamdown`'s nested copy via an `overrides` entry, closing 4 moderate classDef/Gantt advisories), `shell-quote@^1.8.4` override (closed the lone critical). Remaining items are not reachable on Systify's runtime paths or are owned by upstream packages with no compatible patch:

1. **protobufjs (8) & @grpc/grpc-js (2)** — `@daytona/sdk › @opentelemetry/sdk-node › …`. Inert: `createDaytonaClient` never sets `otelEnabled`, so the OTel export path never runs. Patched versions are pinned inside Daytona's OTel stack; forcing them skews a stack the SDK ships and tests against.
2. **fast-xml-builder (1, high)** — `@daytona/sdk › @aws-sdk/lib-storage › … › fast-xml-parser`. Reachable only via Daytona's AWS S3 storage features, off Systify's provision/exec path. Fix needs a major `fast-xml-parser` bump under aws-sdk, risking S3 XML parsing.
3. **esbuild (1, high)** — `convex › esbuild`, `vite › esbuild`. Build tooling, not a runtime dep; owned by Convex and Vite.
4. **fast-uri (2, high)** — `eslint › … › ajv` and `shadcn › @modelcontextprotocol/sdk › ajv`. Dev/CLI tooling, not runtime; the shadcn path is plan 002's concern.
5. **ws (moderate)** — `convex › ws`, `@daytona/sdk › isomorphic-ws › ws`. Transitive, owned by Convex and Daytona; no direct dependency to bump.
6. **uuid (moderate)** — `mermaid › uuid`. Nested inside `mermaid@11.15.0`; the vuln only triggers for `v3/v5/v6` calls with a `buf` arg, which mermaid does not use. No mermaid release pins a patched uuid yet; a `uuid` major override is riskier than the moderate it would close.

## Findings considered and rejected

- `.deepsec` finding "Clone failure wrapper can persist unredacted credential-bearing SDK errors": rejected as stale for current code. `convex/daytona.ts` now sanitizes clone diagnostics through `redact()` before constructing wrapped messages and causes.
- `.deepsec` finding "Sandbox redaction misses common credential formats": rejected as stale for current code. `convex/chat/redaction.ts` now includes OpenAI, Anthropic, Google, private key, database URL, credential URL, AWS secret, Slack, assignment-style, Basic, Bearer, JWT, and GitHub token patterns, with tests in `convex/chat/redaction.test.ts`.
- Test output warning "Window's scrollTo() method not implemented": not planned. It is a JSDOM environment warning with passing tests and no evidence of production impact.
- Test output warning `TimeoutNegativeWarning`: not planned. It did not fail tests, and no specific production call site was confirmed during the audit.

### 2026-06-18 full audit — vetted and rejected (don't re-audit)

A bare `/improve` run re-verified the entire `.deepsec/findings/` backlog (~30 leads from the 2026-06-04 scan) and `VERCEL_AI_AUDIT.md` against HEAD `d69ad4a`. Nearly all are fixed or by-design. Confirmed rejected by reading the live code:

- **Cross-tenant / access-control leads (all fixed):** `saveInstallation` is now an `internalMutation` enforcing one-active-owner-per-installation + GitHub-user verification (`convex/github.ts:321-348`); `createThread` requires repo ownership via `requireActiveRepositoryForViewer`; `getThreadContext` authenticates before the existence probe (`loadOwnedDoc`); scoped artifacts are re-validated against the thread's repository in `replyGroundingNode`.
- **Resource-bound / rate-limit leads (all fixed/by-design):** GitHub actions consume server-side rate limits and cap pagination; `checkForUpdates` reserves the throttle before the outbound call; the GitHub webhook has a 1 MiB capped reader; sandbox tools size-check before download; `getCachedSelectionStatus` dedups; `getUserCostBreakdown` uses a time-bounded index; folder caps enforced at create/move/remove.
- **Job-lifecycle leads (fixed/by-design):** provisioning re-checks repo+sandbox state in attach/markReady; import jobs are leased + recovered; deletion bounds cleanup retries and fails gracefully (`convex/repositories-delete.test.ts:585-623`); clone errors are redacted. The remaining "zombie cleanup job rows never marked failed" is cosmetic — `listActiveCleanupJobs` filters by lease expiry, so it blocks nothing.
- **LLM/model leads (fixed/by-design):** embedding dims aligned via `embeddingDimensions` in the catalog; the gateway asserts capability; `isValidPick` is internal-only and every public surface uses `isUserPickableModel`; prompts include history; eval uses `repo.ownerTokenIdentifier`. `parseGitHubUrl` dropping multi-segment `/tree/a/b` branches is an intentional, tested tradeoff (`convex/github.test.ts:132` "ignores subtree paths").
- **`scripts/evalSystemDesign.ts` `--budgets` fail-fast:** already implemented — `parseBudgetsArg` validates each token (`/^[1-9][0-9]*$/`) and throws on invalid/empty input.
- **Direction candidates that turned out already-shipped:** shareable Library Ask threads (shareable from `history.tsx` regardless of mode); "ask about this artifact" (Library Ask auto-scopes a new thread to `activeArtifactId`, `library-ask-panel.tsx:285`); vector RAG in Discuss-Library grounding (`replyGroundingNode.ts:64` calls `retrieveArtifactChunks`); System Design freshness-on-sync (the import-drift signal `alignedImportCommitSha → importDriftFromLatestSync`, surfaced in `FolderNavigator`).
- **Test-coverage gaps (over-reported, rejected):** `requestSandboxActivation` is tested (`convex/sandboxActivation.test.ts:171`), `use-thread-capabilities` has a test (`src/hooks/use-thread-capabilities.test.tsx`), and the rate-limit cost-cap math is tested via `peekSandboxDailyCostForUser` in `convex/lib/usageAccounting.test.ts:120` and `convex/systemDesign.test.ts:321`.

The only genuinely-open items are low-leverage (a dedicated `convex/lib/rateLimit.test.ts` file; minor UX niceties) or a large deliberately-deferred feature (**path-scoped artifact drift** — see `docs/repository/artifact-import-drift-system-design.md` "Future Work"). None were worth a plan.

### 2026-06-24 HTML-artifact-versioning audit — vetted and rejected (don't re-audit)

Checked against HEAD `c7b6aac`. Rejected after reading the live code:

- **Mermaid repair returns version from the pre-update read** (`convex/artifactMermaidRepair.ts:195,229`): by-design. Convex mutations are transactional, so the computed `artifact.version + 1` is correct.
- **Missing `ownerTokenIdentifier` re-check on `currentVersionId` / `listByArtifact`** (`artifactHtml.ts:76-79`, `artifactWrites.ts:461-463`, `artifactVersions.ts:10-24`): defense-in-depth only. Artifact ownership is enforced upstream via `loadOwnedDoc`, and versions are always written with the artifact's owner. LOW; not worth a plan.
- **Version metadata "broadcast" to the client on every artifact view**: mitigated — `listByArtifact` strips `contentMarkdown` (`toVersionMetadata`) and `.take(50)`-caps the rows. Not a finding.
- **Draft-usage settlement duplication** (`settleArtifactDraftUsage` vs `settleFailedDraftUsage`): both route through the shared `settleUsageLifecycleInMutation` (`convex/lib/usageAccountingMutations.ts:87`); divergence risk is low. The `4503648` commit explicitly "standardized draft usage settlement." Not worth a plan.
- **Markdown drafts don't cite artifact versions while HTML drafts do**: UX nicety, no data-integrity cost. Not planned.
- **Subagent claim that version pruning "exists on branch `aa0f183`, just unmerged"**: unverified/incorrect — no pruning exists on `main` or any reachable branch. The real gap (no retention) is captured as plan 006; the branch reference was disregarded.
